---
const { user } = Astro.locals;
---

<div class="glass-card text-foreground/80 mb-4 flex items-center justify-between px-4 py-2 text-sm">
  {
    user ? (
      <>
        <span class="text-muted-foreground">{user.email}</span>
        <div class="flex items-center gap-3">
          <a href="/campaigns" class="text-primary hover:text-primary/80 transition-colors hover:underline">
            Campaigns
          </a>
          <form method="POST" action="/api/auth/signout">
            <button type="submit" class="text-primary hover:text-primary/80 transition-colors hover:underline">
              Sign out
            </button>
          </form>
        </div>
      </>
    ) : (
      <>
        <span class="text-foreground font-medium">ContentForge</span>
        <div class="flex gap-3">
          <a href="/auth/signin" class="text-primary hover:text-primary/80 transition-colors hover:underline">
            Sign in
          </a>
          <a href="/auth/signup" class="text-primary hover:text-primary/80 transition-colors hover:underline">
            Sign up
          </a>
        </div>
      </>
    )
  }
</div>
